# M6 — Documentation and release readiness

Ordered checklist; spec references are to `SPECIFICATION.md`. This is the final milestone: no
new library behaviour, only documentation and publishing configuration.

- [x] **1. `README.md`**
  - Quick start (a runnable-looking snippet: build an `AlterEgo`, bind/apply a couple of
    built-ins).
  - The determinism model in one paragraph (section 3.1: derived per input, order-independent,
    parallel-safe).
  - The "this is pseudonymisation, not anonymisation — guard the salt" warning, including the
    frequency-preservation and low-cardinality limits (section 3.3).
  - The fictional-by-default guarantee table (section 4.1: email/phone/postcode, mechanism,
    opt-out).
  - The `unique()` order-independence caveat (section 5.3: order-independent absent a collision;
    on collision, whichever input is processed first keeps the natural candidate).
  - A record-coherence example (section 6.1's own `try (RecordScope rec = ...)` shape).
  - An extension example (section 7's custom-strategy shape).
  - Licence section: MIT, pointing at `LICENCE`.
  - Data-attribution section: dictionary data is OGL-licensed UK government data
    (`docs/dictionaries.md`), pointing at `NOTICE`; states the OGL attribution obligation passes
    through transitively to applications depending on AlterEgo, since the JAR bundles the data.
  - **Code samples compiled as tests, so they cannot rot**: an `src/test/java` class containing
    each README snippet verbatim (or as close as a compilable snippet allows), so a future
    breaking change to the public API fails a test here before it ever reaches a release.
  - Acceptance: every README code block has a corresponding compiled-and-run test; `./gradlew
    build` green.

- [x] **2. Root `NOTICE` completeness check**
  - **Gap found while starting this milestone**: `NOTICE` currently lists only the OGL-licensed
    M2 dictionary sources; M3's `dictionaries/GB/phone-ranges.txt` (Ofcom copyright, a
    documented non-OGL exception — `docs/phone-ranges.md`) was never added. Since `NOTICE` is
    packaged into the built JAR and is what downstream applications actually see, add its
    attribution now, before README's data-attribution section points readers at it.
  - Acceptance: `NOTICE` covers every dictionary-adjacent resource actually shipped, including
    `phone-ranges.txt`; `unzip -l` on the built jar still shows `META-INF/NOTICE` (regression
    check, already covered since M2 — just confirm it still holds).

- [x] **3. Javadoc**
  - `./gradlew javadoc` warning-free — already enforced (`-Xwerror`) and green as of M5; no new
    work expected here beyond covering whatever this milestone's own new code adds (the README
    test class is test-scope, not part of the public API surface, so shouldn't add any).
  - Acceptance: `./gradlew javadoc` green.

- [x] **4. `maven-publish` configuration**
  - Group id `io.github.dconneely` (already set); artifact id `alterego` (matches the module
    name and the library's own identity, distinct from the `lib-alterego` repository/directory
    name); licence MIT in the POM, pointing at the project's GitHub repository.
  - Sources and Javadoc jars, since Maven Central requires both for a release.
  - No repository/credentials configuration committed — that's environment-specific and not
    this milestone's job; `maven-publish` only needs to produce a correct, complete POM and the
    three artifact jars (binary, sources, javadoc) locally (`./gradlew publishToMavenLocal`).
  - Acceptance: `./gradlew publishToMavenLocal` succeeds; the installed POM has the correct
    groupId/artifactId/licence/SCM metadata; the built jar still has `META-INF/LICENCE` and
    `META-INF/NOTICE` (regression, from M2).

- [x] **5. `CHANGELOG.md`**
  - Notes the output-stability guarantees (section 3.4: dictionaries versioned, Appendix A
    frozen within a major version, golden outputs pin exact values) and that the conformance
    vectors under `src/test/resources/vectors/` are frozen.
  - An entry for the (still unreleased) `0.1.0` version, since this is pre-1.0 and nothing has
    shipped yet — dated with today's date, not a placeholder.
  - Acceptance: `CHANGELOG.md` exists at the repo root, referenced from `README.md`.

- [x] **6. Milestone test gates**
  - Acceptance: `./gradlew build` green; `./gradlew javadoc` green; `./gradlew
    publishToMavenLocal` succeeds; all M6 "done when" criteria in `PLAN.md` met.
