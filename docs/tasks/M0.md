# M0 — Project scaffold and implementer guardrails

Ordered checklist. Complete each item and verify its acceptance command before moving on.
See `PLAN.md` for scope.

- [x] `docs/adr/0001`–`0008` — decision records. *(already created)*

- [x] **Gradle scaffold**
  - `settings.gradle` — `rootProject.name = 'lib-alterego'`.
  - `build.gradle` — Groovy DSL: `java-library` plugin; `group = 'io.github.dconneely'`;
    toolchain `languageVersion = JavaLanguageVersion.of(25)`; UTF-8 encoding;
    `testImplementation` for JUnit Jupiter and jqwik; `useJUnitPlatform()`; test logging shows
    failed-test output.
  - Gradle wrapper, current 9.x: `gradle wrapper --gradle-version <latest 9.x>`. Generated with
    9.6.1 via a local SDKMAN-installed Gradle (no system Gradle available); all subsequent
    commands use `./gradlew`.
  - Acceptance: `./gradlew build` succeeds (no sources yet is fine). *(verified)*

- [x] **`.gitignore`** — `.gradle/`, `build/`, `bin/`, `.idea/`, `*.iml`, `.DS_Store`,
  `.jqwik-database`. The last two entries were added after a stray IDE `bin/` output directory
  and jqwik's own state file showed up in `git status`; neither is a build artifact any task
  asked for.

- [x] **Package skeleton + JPMS**
  - `src/main/java/module-info.java` declares `module io.github.dconneely.alterego { }` with
    **no exports yet**: javac rejects `exports` of a package containing only
    `package-info.java` ("package is empty or does not exist"), so `io.github.dconneely.alterego`
    and `io.github.dconneely.alterego.store` cannot be exported until M1 step 1 adds real public
    types to them. M1 step 1 must add
    `exports io.github.dconneely.alterego;` and `exports io.github.dconneely.alterego.store;` at
    that point — do not forget this step.
  - Create packages (with `package-info.java` each, one-line Javadoc):
    - `io.github.dconneely.alterego`
    - `io.github.dconneely.alterego.store`
    - `io.github.dconneely.alterego.strategy`
    - `io.github.dconneely.alterego.pattern`
  - Note: `strategy` and `pattern` are never exported — internal packages.
  - Acceptance: `./gradlew build` green. *(verified)*

- [x] **Placeholder test** — `src/test/java/.../SmokeTest.java` asserting the module compiles
  and loads (e.g. `Class.forName` on a package-info or a trivial assertion).
  - Acceptance: `./gradlew test` runs 1 test, green. *(verified)*

- [x] **CI** — `.github/workflows/ci.yml`: on push and pull_request; checkout;
  `actions/setup-java` (Temurin 25); `./gradlew build`.
  - Acceptance: workflow file is valid YAML *(verified locally)*; CI green on next push
    (human pushes — not yet triggered, since nothing is committed).

- [x] **`README.md` stub** — one paragraph on what AlterEgo is, links to `SPECIFICATION.md` and
  `PLAN.md`, and a "not yet released" note. Wrap at column 100.

**Milestone done when**: `./gradlew build` passes locally and in CI with one placeholder test.
Locally verified; CI will confirm on the human's next push.
