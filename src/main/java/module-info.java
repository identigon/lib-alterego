// M1 restores the exports below once these packages contain a public type: javac rejects
// `exports` for a package that holds only package-info.java (see docs/tasks/M0.md).
module io.github.dconneely.alterego {
}
